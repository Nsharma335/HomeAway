1. downloaded folder
2. Install node from https://nodejs.org/en/ v-8.12.0 so that terminal detect it
3. Npm install (if already packgjson created it installs dependency) and then npm init(creates package son)
4. Nom install express -save
5. npm install -g nodemon
6. New install mongoldb
7. and robot 3t - https://robomongo.org/
8. Install mongo driver npm install mongodb --save